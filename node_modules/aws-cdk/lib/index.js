"use strict";
const path = require('path');

function initTemplatesDir() {
  return path.join(__dirname, 'init-templates');
}

class CDK {
  constructor(opts) {
    this.opts = opts || {};
  }

  templates() {
    return initTemplatesDir();
  }
}

module.exports = { CDK, initTemplatesDir };
